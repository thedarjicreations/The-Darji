# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to git!

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=5000
FRONTEND_URL=https://your-domain.com

# Database (MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/the-darji-app?retryWrites=true&w=majority

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# AWS S3 Configuration (Optional - remove if using local storage)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_S3_BUCKET=your-bucket-name

# CORS Configuration
CORS_ORIGIN=https://your-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Email/WhatsApp (Optional)
WHATSAPP_API_KEY=your-whatsapp-api-key
WHATSAPP_PHONE_NUMBER=+1234567890

# Backup Configuration (Optional)
BACKUP_ENABLED=true
BACKUP_CRON_SCHEDULE=0 2 * * *

# ========================================
# SECURITY NOTES:
# ========================================
# 1. Use strong, random JWT_SECRET (min 32 characters)
# 2. Use MongoDB Atlas with IP whitelist
# 3. Enable 2FA on all cloud accounts
# 4. Rotate secrets regularly
# 5. Use environment-specific secrets
# ========================================
